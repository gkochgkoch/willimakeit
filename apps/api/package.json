{
  "name": "@willimakeit/api",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn willimakeit.main:app --reload",
    "lint": "uv run ruff check src tests",
    "typecheck": "uv run mypy src"
  }
}
